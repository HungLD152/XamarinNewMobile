<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SmartNews.Views.SettingView">
    <StackLayout Orientation="Horizontal" Margin="0,5,10,-5">
        <!--<SearchBar x:Name="searchBar" Text="{Binding searchText}"
                        Placeholder="Search items..."
                        HorizontalOptions="Fill"
                        VerticalOptions="StartAndExpand"
                        FontAttributes="Italic"
                        SearchButtonPressed="OnSearchButtonPressed"/>-->
        <ImageButton Source="settingHome.png" WidthRequest="20" Clicked="OnSettingButtonPressed" HorizontalOptions="EndAndExpand"/>
    </StackLayout>
</ContentView>
